// // Assumes that the first two characters (<%) already match
// // Returns the end of the ejs block
// export default function parseEjs(state, start, disableNested) {
//     let level, found, marker, prevPos,
//         labelEnd = -1,
//         max = state.posMax,
//         oldPos = state.pos,
//         quoteRegex = /([`'"])(?:.|[\r\n])*?(?<!\\)\1/mg;

//     state.pos = start + 2;
//     level = 1;
//     while (state.pos < max) {
//         marker = state.src.
//     }
// }